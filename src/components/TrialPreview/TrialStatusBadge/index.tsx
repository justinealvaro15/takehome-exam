import './index.css'

const TrialStatusBadge = () => {
    return (
        <div className="trial-status-pill-root">
            <p className="trial-status-pill-label">LIVE</p>
        </div>
    );
};

export default TrialStatusBadge;
